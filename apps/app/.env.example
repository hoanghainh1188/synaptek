# Supabase (local: lấy từ `supabase start` → `supabase status`).
# Bỏ trống / không có .env → app chạy CHẾ ĐỘ KHÁCH (luyện tập được, KHÔNG lưu tiến độ).
EXPO_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
EXPO_PUBLIC_SUPABASE_ANON_KEY=
